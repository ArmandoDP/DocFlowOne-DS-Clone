---
export interface Props {
  title: string;
  subtitle: string;
  hasAI?: boolean;
  aiIcon?: string;
}

const {
  title,
  subtitle,
  hasAI = false,
  aiIcon = "/src/assets/images/docflowone/IA-icon.png",
} = Astro.props;
---

<div class="mt-16 text-center">
  <h3
    class="flex items-center justify-center gap-3 text-2xl font-bold text-white md:text-3xl">
    {title}
    {
      hasAI && (
        <img
          src={aiIcon}
          alt="IA"
          class="inline-block h-10 w-10 object-contain md:h-10 md:w-10"
        />
      )
    }
  </h3>
  <p
    class="mx-auto mt-3 max-w-2xl text-base leading-relaxed text-gray-300 md:text-lg">
    {subtitle}
  </p>
</div>
