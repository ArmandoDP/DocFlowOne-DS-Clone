---
# Pour mettre à jour les informations de contact comme email, téléphone, adresse, etc., merci de modifier la table `settings.contactInfo` dans `src/config/config.toml`

enable: true # Contrôle la visibilité de cette section sur toutes les pages où elle est utilisée
titleSize: "display-3" # Si votre titre est plus long, utilisez une taille de texte plus petite comme "display-3", "display-2" ou "display-1"
title: "Automatisez, numérisez et optimisez vos processus opérationnels avec DocFlow One®"

image: "/images/banner/startup.png"
video:
  enable: true
  src: "/videos/VideoDocFlowOneCorto-3.mp4"
  autoplay: true
  loop: true
  muted: true
  playsinline: true

description: "Numérisez et automatisez toute votre gestion documentaire afin que votre équipe travaille efficacement et que vos clients bénéficient d’une meilleure expérience."

button:
  # Consultez le schéma `sharedButton` dans `src/sections.schema.ts` pour toutes les options de configuration disponibles (ex. enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: true
  label: "Découvrez comment cela fonctionne"
  url: "/docflowone"
  # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
  # variant: "" # Optional: fill | outline | text | circle
  # rel: "" # Optional
  # target: "" # Optional
---
